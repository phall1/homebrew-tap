name: Update phui

on:
  repository_dispatch:
    types: [phui-release]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Render formula
        env:
          VERSION: ${{ github.event.client_payload.version }}
          TAG: ${{ github.event.client_payload.tag }}
          DARWIN_ARM64_SHA256: ${{ github.event.client_payload.darwin_arm64_sha256 }}
          DARWIN_X64_SHA256: ${{ github.event.client_payload.darwin_x64_sha256 }}
          LINUX_ARM64_SHA256: ${{ github.event.client_payload.linux_arm64_sha256 }}
          LINUX_X64_SHA256: ${{ github.event.client_payload.linux_x64_sha256 }}
        run: |
          mkdir -p Formula
          cat > Formula/phui.rb <<RUBY
          class Phui < Formula
            desc "Terminal UI for GitHub pull requests, issues, diffs, and Actions"
            homepage "https://github.com/phall1/phui"
            version "${VERSION}"

            on_macos do
              if Hardware::CPU.arm?
                url "https://github.com/phall1/phui/releases/download/${TAG}/phui-darwin-arm64.tar.gz"
                sha256 "${DARWIN_ARM64_SHA256}"
              else
                url "https://github.com/phall1/phui/releases/download/${TAG}/phui-darwin-x64.tar.gz"
                sha256 "${DARWIN_X64_SHA256}"
              end
            end

            on_linux do
              if Hardware::CPU.arm?
                url "https://github.com/phall1/phui/releases/download/${TAG}/phui-linux-arm64.tar.gz"
                sha256 "${LINUX_ARM64_SHA256}"
              else
                url "https://github.com/phall1/phui/releases/download/${TAG}/phui-linux-x64.tar.gz"
                sha256 "${LINUX_X64_SHA256}"
              end
            end

            def install
              bin.install "phui"
            end

            test do
              assert_match version.to_s, shell_output("#{bin}/phui --version")
            end
          end
          RUBY

      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "Brew formula update for phui version ${{ github.event.client_payload.tag }}"
          file_pattern: Formula/phui.rb
